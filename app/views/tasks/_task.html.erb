<table class="table table-striped table-dark">
  <tbody>
    <tr>
      <td><%= link_to task.title, edit_task_path(task), remote: true %></td>
      <td><%= link_to 'Deletar', task, method: :delete, remote: true, class: 'delete' %></td>
    </tr>
  </tbody>
</table>

