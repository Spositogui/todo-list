<%= form_with model: @task do |f| %>
  <%= f.label :title, 'Título' %>
  <%= f.text_field :title %>
  <%= f.label :description, 'Descrição' %>
  <%= f.text_area :description %>
  <%= f.submit @task.new_record? ? 'Criar' : 'Salvar', class: 'btn btn-primary' %>
<% end %>